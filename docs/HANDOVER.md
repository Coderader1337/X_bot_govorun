# Передача: блокеры и риски

Проверено 31.08.2026 (VPS только чтение). Контейнеры тогда не трогали.

## Блокеры

1. **VPS общий** — RAG, Qdrant, почта. Root заказчику лид-бота не отдавать; нужен отдельный сервер или ограниченный пользователь.
2. **Кабинеты X и DeepSeek** не подтверждены как «заказчик входит сам». Ключи в `.env` ≠ логин портала.
3. **Секреты в git-истории** (gitleaks 8.30.1, 8 находок): `.env_reserve.txt` в `ee0ed37`, пароль VPS в старых `scripts/sync_to_vps.py` / `reset_vps_db.py`. HEAD скриптов чистый. Нужна ротация ключей и новый репозиторий без этой истории.
4. **402 credits depleted** с 27.08.2026 — без оплаты бот жив, но не постит.
5. GitHub и SSH на разработчике.

## Слабые места

| Риск | Суть |
|---|---|
| Холостой daemon | `discovery_complete=1` + пустая очередь не возобновляет поиск |
| Текст константа | X режет дубликаты |
| Нет бэкапа SQLite | История постов в одном файле |
| Search/all | Платный, курсор на плотных окнах врёт |
| Нет алертов | 402 / очередь 0 никто не пишет в мессенджер |

Наивный `docker compose up -d` больше не поднимает search-бот: в репозитории остался один сервис. На VPS сирота `movetorussia_twitter_bot` (Exited) может ещё лежать, пока её не удалят (`docker rm`, см. [05-vps-deploy.md](05-vps-deploy.md)).

## Сдача

1. Биллинг X и DeepSeek на заказчика.
2. Новые ключи, `.env` на VPS, recreate контейнера.
3. Новый пароль SSH; отдельный VPS или пользователь.
4. Git без секретов в истории + эта документация.
5. Совместный `--once --dry-run`, затем живой цикл после кредитов.
